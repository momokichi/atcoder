#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define _GLIBCXX_DEBUG

int main()
{
  int t;
  cin >> t;
  while (t--)
  {
    int n, a, b, c, d;
    cin >> n >> a >> b >> c >> d;
    }
}
